# Let a tuple (x, y) represent a point in a Cartesian system.
# Write a function that receives as a parameter a list of
# points and returns a list of unique tuples (a, b, c)
# representing the unique lines determined by those points
# ((a, b, c) corresponds to the right ax + by + c  = 0).